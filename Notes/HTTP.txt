Структура HTTP запиту			| GET - метод запиту
								| /home/index?x=10 - URL
GET /home/index HTTP/1.1		| /home/path - path
Host: localhost					| ?x=10 - Query
Connection: close				| Host, Connection - заголовки (headers)
								| Ключ: значення\r\n
Структура відповіді				| HTTP/1.1 - протокол (scheme)
								|
HTTP/1.1 200 OK					| 200 - status code
Connection: close				| OK - reason phrase (status message)
Content-type: text/html			| Заголовки 
Content-length: 100500			|
								| порожний рядок (\r\n)
<!doctype html> ...				| Тіло (body)

--------------------------------------------------------------------------------
Особливості:
Метод запиту - перше "слово" у пакеті, є 9 стандартніх методів, також 
існують позастандартні, більшість серверів не обмежують нестандартні методи. 

Заголовки: більшість універсальні, але деякі мають спецпризначення
- тальки у запиті
- тільки у відповіді
- тільки з відповідним статус-кодом
	(наприклад, при статусі 401 "Unathorized" має бути посилання на авторізацію
	при статусі 301/302 "Redirect" - має бути нова адреса Location)

Тіло може бути як у запиті, так і у відповіді. За винятками:
запити методами GET та HEAD не повинні мати тіло. Цей факт браузер блокує.
